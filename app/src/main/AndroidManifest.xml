<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.dsvk">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DSVK">
        <activity android:name=".Place_Order"></activity>
        <activity android:name=".CartFood" />
        <activity android:name=".CartActivity" />
        <activity android:name=".DishActivity" />
        <activity android:name=".dish" />
        <activity android:name=".FavouriteFood" />
        <activity android:name=".About" />
        <activity
            android:name=".MenuRecyclerViewAdapter"
            tools:ignore="Instantiatable" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".HomePage" />
        <activity android:name=".Register_Details" />
        <activity android:name=".Register" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>